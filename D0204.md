# 스택, 힙 메모리란 무엇이며 어떤 차이가 있는지 비교해서 설명하세요.

## 메모리
지금 당장 실행 중인 프로그램이 쓰는 데이터를 잠시 올려두는 장소

## 메모리 구역
- 코드(Code) 영역: 우리가 짠 소스코드 자체가 기계어로 저장되는 곳.

- 데이터(Data) / 정적(Static) 영역: 전역 변수와 정적(static) 변수가 저장되는 곳.

- 힙(Heap) 영역: new로 생성한 객체들이 사는 곳 (동적 할당).

- 스택(Stack) 영역: 지역 변수와 매개변수가 사는 곳 (잠시 머무름).




## 스택
가장 나중에 들어온 데이터가 가장 먼저 나가는 구조
### 용도: 
- 지역변수, 매개변수, 함수의 리턴 주소 등 잠깐 쓰고 버릴 데이터 저장
- 
### 특징: 
- 함수가 실행될 때 할당되고, 함수가 종료되면 자동으로 메모리에서 사라짐
- 메모리 할당속도가 매우 빠름
- 공간이 매우 제한적, 너무 깊은 재귀함수를 돌리면 스택이 꽉차서 스택 오버플로우 발생

## 힙
무더기, 원할 때 자유롭게 공간을 빌리고 반납
### 용도: 
- 동적으로 할당되는 데이터(객체, 리스트 등)를 저장
### 특징: 
- 사용자가 직접 크기를 정하고 관리
- 스택에 비해 메모리 할당속도가 매우 느림,
- 메모리 누수 발생할 수 있어

| 구분 | 스택 (Stack) | 힙 (Heap) |
| :--- | :--- | :--- |
| **관리 주체** | OS/컴파일러 (자동) | 개발자/런타임 (수동/GC) |
| **속도** | 매우 빠름 | 상대적으로 느림 |
| **크기** | 작고 제한적임 | 크고 유연함 |
| **데이터 수명** | 함수 종료 시 해제 | 명시적 해제 전까지 유지 |
| **주요 에러** | Stack Overflow | Memory Leak, Out of Memory |

```dart
void memoryTest() {
  // 1. 스택(Stack) 영역
  // 숫자, 불리언 같은 기본 타입의 '변수 이름'과 '값'이 저장됩니다.
  int age = 25;
  
  // 2. 힙(Heap) 영역
  // 'List', 'Map', 혹은 우리가 만든 'Widget', 'Class' 등은 모두 힙에 생성됩니다.
  List<String> friends = ['철수', '영희']; 
  
  // [메모리 내부 모습]
  // age (스택): 25라는 값을 직접 가짐
  // friends (스택): 힙에 있는 리스트 객체의 "주소(포인터)"를 가짐
  // ['철수', '영희'] (힙): 실제 리스트 데이터가 덩어리로 저장됨
}
```

함수가 끝나자마자 스택에서 할당된 메모리가 해제됩니다.
age: 스택에 존재 (값: 25) -> 삭제
friends: 스택에 존재 (값: 힙에 저장된 리스트의 '주소값') -> 삭제

하지만 힙 영역에 저장된 값 friends는 함수가 끝나도 메모리에 그대로 남아있습니다. * C++ 같은 언어: 개발자가 delete 명령어로 직접 치워주지 않으면 프로그램이 끝날 때까지 메모리를 차지합니다. (이게 쌓이면 메모리 누수가 됩니다!)
friends: 실제 리스트 데이터는 힙에 존재 -> 유지
Java/Python 같은 언어: 아무도 이 'friends'를 찾지 않는다는 걸 눈치챈 **가비지 컬렉터(GC)**가 나중에 슬쩍 치워줍니다.

스택은 속도 -> 빨리쓰고 빨리 버리기
힙은 자유도 -> 크게 쓰고 오래 들고있기

- 스택만 쓸 경우: 프로그램 실행 중에 데이터 크기가 커지면(예: 고화질 이미지 로드) 대응할 방법이 없어 프로그램이 바로 꺼집니다.

- 힙만 쓸 경우: 아주 단순한 숫자 계산을 할 때도 메모리 빈 공간을 찾고, 나중에 그걸 또 직접 치워줘야 해서 스마트폰 배터리와 성능이 순식간에 바닥날 것입니다.

모든 걸 힙에 넣으면 프로그램이 너무 느려지고, 모든 걸 스택에 넣으면 유연성이 사라집니다.