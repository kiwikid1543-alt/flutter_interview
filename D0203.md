# 얕은복사와 깊은복사의 차이

## 복사
다트에서 객체는 메모리 주소를 가리키는 참조 방식을 전달
### 불변성
데이터를 직접 수정(mutable)하는 대신, 원본은 그대로 두고 수정된 사본을 사용하면 데이터의 흐름을 예측하기 쉬움
특히 앱이 커질수록 여러 화면에서 같은 데이터를 공유하게 되는데, 한곳에서 몰래 바꾼 데이터 때문에 다른 화면이 망가지는 것을 방지
### 상태관리

## 메모리 주소 가리키기
데이터 저장할 때
객체는 덩치가 커서 변수 상자에 직접 넣기 어렵기 때문에, 메모리 상에 객체를 저장하고 그 객체가 저장된 메모리 주소를 변수에 저장


## 얕은 복사
- 메모리 주소 공유

- 가장 바깥쪽의 껍데기(객체 자체)만 새로 만드는 방식
-  만약 객체 내부에 리스트나 다른 객체가 포함되어 있다면, 그 내부 요소들은 원본과 복사본이 같은 메모리 주소를 공유
- 복사 속도가 빠르지만, 내부 객체를 수정하면 원본과 복사본이 동시에 변하는 문제가 발생



## 깊은 복사
- 새로운 복사본을 만들어서 메모리 주소를 공유하지 않음

- 내부에 중첩된 모든 객체들까지 전부 새로 생성하는 방식
- 원본과 복사본 사이에 공유되는 메모리가 전혀 없도록 완전히 독립적인 개체를 만듭니다.
- 복사 속도가 상대적으로 느리지만, 내부 객체를 수정해도 원본에 전혀 영향을 주지 않아 안전합니다.



# 얕은 복사(Shallow Copy) vs 깊은 복사(Deep Copy)

| 구분 | 얕은 복사 (Shallow Copy) | 깊은 복사 (Deep Copy) |
| :--- | :--- | :--- |
| **복사 범위** | 최상위 객체만 | 내부의 모든 중첩 객체 포함 |
| **참조 관계** | 내부 객체는 원본과 주소를 공유 | 모든 객체가 새로운 주소를 가짐 |
| **속도** | 빠름 | 상대적으로 느림 |
| **안전성** | 의도치 않은 변경 가능성 있음 | 완벽하게 독립적임 |

스택과 힙
실제 객체가 저장된는곳 
함수 변수가 통용되는 곳
그림자료 스택과 힙
