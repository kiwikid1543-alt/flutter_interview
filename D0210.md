# 🚀 Dart 클래스 및 접근 제한자 심층 가이드

## 1. 클래스의 종류 (Class Types & Modifiers)

Dart는 객체 지향 언어로서 다양한 클래스 형태를 제공하며, 특히 **Dart 3.0**부터는 클래스의 권한을 정교하게 제어할 수 있는 'Modifiers'가 추가되었습니다.

### 🔹 기본 클래스 형태
*   **일반 클래스 (`class`)**: 가장 일반형입니다. 인스턴스 생성이 가능하며, 다른 클래스에서 상속(`extends`)되거나 인터페이스로서 구현(`implements`)될 수 있습니다.
*   **추상 클래스 (`abstract class`)**: 직접적인 인스턴스 생성을 막고, 하위 클래스에서 반드시 구현해야 할 추상 메서드를 정의하는 '설계도' 역할을 합니다.
*   **믹스인 (`mixin`)**: 다중 상속의 위험성 없이 코드를 여러 클래스 계층에서 재사용하기 위한 조각입니다. `with` 키워드를 통해 기능을 주입하며, 생성자를 가질 수 없습니다.
*   **열거형 (`enum`)**: 고정된 수의 상수 값을 정의합니다. Dart의 Enum은 메서드와 필드를 가질 수 있는 **Enhanced Enums** 기능을 지원하여 복잡한 상태 정의에 유리합니다.

### 🔹 [Advanced] 클래스 수정자 (Dart 3.0+)
대규모 프로젝트에서 아키텍처의 안정성을 확보하기 위해 클래스의 사용 범위를 제한합니다.
*   **`sealed` (폐쇄형)**: **가장 권장되는 타입 설계 도구**입니다. 동일한 라이브러리(파일) 내에서만 상속이 가능하며, `switch` 문에서 패턴 매칭을 사용할 때 컴파일러가 '모든 하위 타입을 처리했는지' 검사(Exhaustiveness)해 줍니다.
*   **`final`**: 상속(extends)과 구현(implements)을 모두 금지합니다.
*   **`base`**: 상속은 허용하지만 본래의 구조를 유지하기 위해 인터페이스 구현(`implements`)은 금지합니다.
*   **`interface`**: 상속은 금지하고 인터페이스로서의 구현만 허용합니다.

---

## 2. 접근 제한자 (Access Modifiers)

Dart는 타 언어와 달리 `public`, `private`, `protected` 키워드가 존재하지 않습니다. 대신 **이름의 접두사**로 가시성을 결정합니다.

| 구분 | 선언 방식 | 접근 범위 |
| :--- | :--- | :--- |
| **Public** | `userName`, `fetchData()` | 프로젝트 어디서나 접근 가능 (기본값) |
| **Private** | **`_`**`userName`, **`_`**`fetchData()` | 선언된 **라이브러리(해당 .dart 파일)** 내에서만 접근 가능 |

1. 캡슐화 (Encapsulation) & 정보 은닉
가장 큰 이유는 내부의 복잡한 구현 내용을 밖으로 드러내지 않기 위함입니다.

자동차 아날로그: 운전자는 '핸들'과 '엑셀'만 알면 됩니다. 엔진 내부의 복잡한 동작(private 멤버)까지 운전자가 직접 건드려야 한다면 운전이 너무 위험하고 복잡해지겠죠?
이점: 클래스 외부에서는 꼭 필요한 기능(Public API)만 사용하게 하여 사용법을 단순하게 만듭니다.
2. 데이터 무결성 보장 (Data Integrity)
클래스 내부의 중요한 데이터를 외부에서 마음대로 바꾸지 못하게 보호합니다.

예시: 사용자의 '나이' 변수가 public이라면 외부에서 -100 같은 말도 안 되는 값을 넣을 수 있습니다.
해결: 변수를 private으로 숨기고, setAge(int age) 같은 메서드를 통해서만 바꾸게 하면, 메서드 안에서 "값이 0보다 큰지" 검증 로직을 넣을 수 있어 데이터의 정결함을 유지할 수 있습니다.
3. 유지보수의 유연성 (Decoupling)
나중에 내부 코드를 수정하더라도 외부 코드에 영향을 주지 않기 위함입니다.

상황: 특정 기능을 수행하는 내부 로직(_calculatePrice)을 private으로 만들었다면, 나중에 이 계산 방식을 완전히 갈아엎어도 외부에서 이 클래스를 쓰는 사람들은 아무런 수정 없이 그대로 쓸 수 있습니다.
이점: 내부 구현을 마음껏 변경할 수 있는 '자유'가 생깁니다. 만약 모든 게 public이었다면, 코드 하나 고칠 때마다 이 클래스를 쓰는 모든 곳이 고장 날까 봐 걱정해야 합니다.
4. 코드의 오용 방지 (Explicit Interface)
동료 개발자나 미래의 나에게 **"이건 여기서만 써야 하는 도구야"**라고 명확한 가이드를 주는 것입니다.

가이드: Public 멤버는 "내가 제공하는 공식 기능"이고, Private 멤버는 "내부를 돌리기 위한 부속품"이라는 신호입니다. 이를 통해 다른 개발자가 내부 로직을 잘못 건드려 발생할 수 있는 버그를 원천 봉쇄합니다.

### ⚠️ 실무 핵심 포인트
1.  **Library-level Privacy**: Dart의 `private`은 클래스가 아니라 **'파일 단위'**입니다. 즉, 같은 파일 내에 있다면 클래스가 달라도 서로의 `_` 멤버에 접근할 수 있습니다. 이를 통해 긴밀하게 연결된 클래스들을 하나의 파일에 묶어 안전하게 관리합니다.
2.  **Protected의 실체**: Dart 언어 차원의 `protected` 키워드는 없습니다. 대신 `package:meta`의 **`@protected`** 어노테이션을 사용하여 자식 클래스에서만 접근해야 함을 명시하며, IDE나 린터(Linter)가 이 규칙을 검사해 줍니다.

---

## 3. 생성자 및 클래스 특징

*   **팩토리 생성자 (`factory`)**: 인스턴스를 루틴하게 생성하지 않고 **싱글톤 패턴**을 구현하거나, 상황에 따라 **서브클래스의 인스턴스를 반환**해야 할 때 사용합니다.
*   **상수 생성자 (`const`)**: 컴파일 타임에 인스턴스를 상수로 고정합니다. Flutter에서 위젯의 불필요한 리빌드를 막아 성능을 최적화하는 핵심 요소입니다.
*   **불변 클래스**: 모든 필드를 `final`로 선언하고 `const` 생성자를 제공하여 객체의 상태가 변하지 않음을 보장합니다.

---

### 💡 시니어의 조언
> 입문 단계에서는 `_`를 통한 접근 제어에 집중하되, 중급 이상으로 나아가려면 **`sealed class`**를 활용한 상태 모델링(State Modeling)과 **`factory`** 생성자를 활용한 유연한 인스턴스 관리 기법을 익히는 것이 중요합니다.

https://dart.dev/language/mixins
https://dart.dev/language/class-modifiers

예시

.
클래스
객체를 만드는 설계도

일반
추상- 상속과 구현
믹스인- 조합
열거형- 정해진선택지중하나

접근제한자
클래스 내부 멤버 어디까지 보고 수정? 범위제한

퍼블릭 
프라이빗 _  ->  .dart 파일 안에서만

외부에서 바꾸면 x
내부 로직용 변수나 함수를 숨기기 위해

결합도 감소와 유지보수성


