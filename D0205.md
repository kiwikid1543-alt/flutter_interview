컴퓨터에서 소수점을 계산하는 방식은 무엇인가요?

컴퓨터는 0과 1로 이루어진 이진수체계 사용

## 고정 소수점
고정 소수점은 정수를 표현할 비트 수와 소수를 표현할 비트 수를 미리 정해줍니다. 
(32비트로 소수를 표현한다고 가정)
첫 1비트: 부호를 나타내는 비트입니다. (양수는 0, 음수는 1을 의미합니다.)
다음 15비트: 정수부를 나타내는 비트입니다.
다음 16비트: 소수부를 나타냅니다.
소수점의 위치는 정수부와 소수부의 경계입니다.

고정 소수점이 표현할 수 있는 실수는 한계가 있다. 왜냐하면 정수는 15비트로, 소수는 16비트 안에서 표현해야 하기 때문이다. -> 부동 소수점방식 사용


그러므로 이를 벗어나는 수를 고정 소수점 방식으로 표현하려고 시도하면 ‘정밀도(?)’가 떨어진다.
*정밀도: 정보 손실 없이 나타낼 수 있는 유의한 자릿수 (표현할 수 있는 비트 수 정도로 이해하고 있음)

## 부동 소수점(floating point)
소수점이 옮겨 다니는 실수 표현법이다. 
부동 소수점이 비트를 사용하는 방법과 고정 소수점이 비트를 사용하는 방법이 다릅니다.
1 비트 부호
8비트 지수
23비트 가수부

그리고 부동 소수점의 비트 체계 중 가장 널리 쓰이는 표준은 IEEE 754 라고 합니다. 

1. 2진수를 정규화 합니다.
- 정규화
실수를 1.xx * 2^n의 꼴로 나타내는 것을 정규화라고 합니다.
이 때 N은 지수로, .xxx 는 가수부를 의미합니다.

(32비트로 표현한다고 가정)
2. 처음 1비트는 부호를 나타냅니다.

3. 다음 8비트는 지수부를 나타냅니다. 정규화 과정에서 얻은 지수에 bias(127)를 더한 값으로 채웁니다.
- 127 인 이유 

- 지수가 음수일 경우를 대처하기 위해서
- bias를 더하지 않는다면 지수가 음수인지 양수인지를 표현하는 비트를 새로 할당해야 함

4. 다음 23비트는 가수부를 나타냅니다. 소수 부분의 값으로 채웁니다.

5. 채워지지 않은 비트를 모두 0으로 채웁니다.

7.625를 부동 소수점으로 표현해보겠습니다.
2진수 변환: 111.101
정규화 : 1.11101 * 2^2 (모든 정규화된 이진수는 항상 1로 시작)
지수부: 2 + 127 = 129 = 10000001
가수부: 11101


부동 소수점 방식은 고정 소수점 방식보다 넓은 범위의 수를 나타낼 수 있어서 과학 또는 수학 계산을 할 때 많이 이용됩니다.


## 피드백
꼭 정리!
하지만 근삿값으로 표현된다는 점과 고정 소수점 방식보다 연산 속도가 느리다는 점 때문에 부동 소수점 방식을 위한 별도의 연산 장치를 두는 경우가 많습니다. -> 근삿값으로 어떻게 표현되는가
비트
32비트 = 4바이트 -> 메모리의 기본 단위?
컴퓨터의 표현단위 
단위 정리 -> 비트 바이트 
정수 int 표현할 수 있는범위
자료형이 처리할 수 있는 범위가 있음 얼마만큼의 크기를 다를 수 있는지
