컴퓨터에서 소수점을 계산하는 방식은 무엇인가요?

#### 비트
- 비트: 컴퓨터가 데이터를 처리하는 가장작은 단위, "0이냐 1이냐"를 나타내는 논리적 단위
- 바이트: 비트는 너무 작아사 보통 비트 8개를 묶어 바이트라는 단위로 데이터 관리
- CPU가 한 번에 처리하기 가장 편한 크기(32비트(4바이트))
- 결국 "적당히 정확하면서, 하드웨어 속도도 빠르고, 배터리도 아낄 수 있는" 최적의 타협점이 바로 32비트


#### 컴퓨터의 표현단위 
비트 
바이트
킬로바이트
메가바이트
기가바이트


컴퓨터는 0과 1로 이루어진 이진수체계 사용

## 고정 소수점
고정 소수점은 정수를 표현할 비트 수와 소수를 표현할 비트 수를 미리 정해줍니다. 
(32비트로 소수를 표현한다고 가정)
첫 1비트: 부호를 나타내는 비트입니다. (양수는 0, 음수는 1을 의미합니다.)
다음 15비트: 정수부를 나타내는 비트입니다.
다음 16비트: 소수부를 나타냅니다.
소수점의 위치는 정수부와 소수부의 경계입니다.

고정 소수점이 표현할 수 있는 실수는 한계가 있다. 왜냐하면 정수는 15비트로, 소수는 16비트 안에서 표현해야 하기 때문이다. -> 부동 소수점방식 사용


그러므로 이를 벗어나는 수를 고정 소수점 방식으로 표현하려고 시도하면 ‘정밀도(?)’가 떨어진다.
*정밀도: 정보 손실 없이 나타낼 수 있는 유의한 자릿수 (표현할 수 있는 비트 수 정도로 이해하고 있음)

## 부동 소수점(floating point)
소수점이 옮겨 다니는 실수 표현법이다. 
부동 소수점이 비트를 사용하는 방법과 고정 소수점이 비트를 사용하는 방법이 다릅니다.
1 비트 부호
8비트 지수
23비트 가수부

그리고 부동 소수점의 비트 체계 중 가장 널리 쓰이는 표준은 IEEE 754 라고 합니다. 

1. 2진수를 정규화 합니다.
- 정규화
실수를 1.xx * 2^n의 꼴로 나타내는 것을 정규화라고 합니다.
이 때 N은 지수로, .xxx 는 가수부를 의미합니다.

(32비트로 표현한다고 가정)
2. 처음 1비트는 부호를 나타냅니다.

3. 다음 8비트는 지수부를 나타냅니다. 정규화 과정에서 얻은 지수에 bias(127)를 더한 값으로 채웁니다.
- 127 인 이유 

- 지수가 음수일 경우를 대처하기 위해서
- bias를 더하지 않는다면 지수가 음수인지 양수인지를 표현하는 비트를 새로 할당해야 함
(64비트에서 지수부는 11비트 따라서 바이어스는 그 중간값인 1023을 사용)

4. 다음 23비트는 가수부를 나타냅니다. 소수 부분의 값으로 채웁니다.

5. 채워지지 않은 비트를 모두 0으로 채웁니다.

7.625를 부동 소수점으로 표현해보겠습니다.
2진수 변환: 111.101
정규화 : 1.11101 * 2^2 (모든 정규화된 이진수는 항상 1로 시작)
지수부: 2 + 127 = 129 = 10000001
가수부: 11101


부동 소수점 방식은 고정 소수점 방식보다 넓은 범위의 수를 나타낼 수 있어서 과학 또는 수학 계산을 할 때 많이 이용됩니다.
하지만 근삿값으로 표현된다는 점과 고정 소수점 방식보다 연산 속도가 느리다는 점 때문에 부동 소수점 방식을 위한 별도의 연산 장치를 두는 경우가 많습니다.

#### 근삿값
 -> 근삿값으로 어떻게 표현되는가
마치 10진수에서 $1/3$을 소수로 적으려면 $0.33333...$ 하고 끝없이 이어지듯, 이진수에서도 우리가 흔히 쓰는 $0.1$ 같은 숫자가 무한 소수가 됩니다.

0.1을 이진법으로 변환(소수부분이 끝까지0이 되지 않는 수들은 무한루프)
0.1 * 2 = 0.2
0.2 * 2 = 0.4
0.4 * 2 = 0.8
0.8 * 2 = 1.6
0.6 * 2 = 1.2
0.2 * 2 = 0.4
0.4 * 2 = 0.8
0.8 * 2 = 1.6

0.00011001.......
가수부 23비트까지 담을 수 있음
안 담기는 부분은 버림
-> 버리기에 오차가 생김

## 피드백
정수 int 표현할 수 있는범위
보통 32비트
1비트는 부호비트로 사용 0양수 1음수
최솟값: $-2,147,483,648$ ($-2^{31}$)
최댓값: $2,147,483,647$ ($2^{31} - 1$)
최댓값에서 1을 빼는 이유는 0때문
-> 이 숫자를 넘어가면 더 큰 그릇 사용
-> 최댓값에 1을 더하면 가장 작은 최솟값으로 넘어감

### 자료형 정리

| 분류 | 자료형 | 비트 수 | 담는 내용 |
| :--- | :--- | :--- | :--- |
| 정수 | int | 32 / 64 | 소수점 없는 숫자 |
| 실수 | double | 64 | 소수점 있는 숫자 |
| 문자 | char | 16 | 글자 딱 하나 ('A', '한') |
| 논리 | bool | 8 | 맞다 / 틀리다 |
| 문자열 | String | 가변 | 글자 덩어리 ("Flutter") |


