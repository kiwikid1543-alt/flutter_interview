# Q.Flutter의 Impeller 렌더링 엔진이란 무엇이며, 기존 Skia와 어떤 차이가 있나요?

## Impeller 렌더링 엔진
플러터 앱의 화면끊김 현상을 근본적으로 해결하기 위해 그래픽 계산을 앱 실행 전에 미리 끝내놓도록 설계된 렌더링 엔진

## 렌더링
컴퓨터 프로그램이 디지털 정보(코드, 숫자, 좌표)를 사람이 눈으로 볼수 있는 영상이나 이미지로 (픽셀)변환하는 과정

## 셰이더
렌더링 과정 중 실행되는 프로그램
그래픽카드가 화면의 점 하나하나를 어떤색으로 칠할지 결정하는 작은 수학프로그램

## 컴파일
사람이 쓴 어어를 기계가 이해할 수 있는 언어로 번역하는 과정

## 엔진
Dart코드를 실제 기기의 화면에 그림으로 그려내고 사용자의 터치입력을 처리하는 핵심 구동부



## Skia와 차이
기존 Skia엔진은 앱이 실행되는 도중에 그래픽 계산식(Shader)을 만듬, 
복잡한 애니메이션이 실행될 때 셰이더를 실시간으로 컴파일(번역)하면서 프레임이 끊기는 현상(Jank)이 발생

Skia가 JIT 였던 이유: GPU마다 기계어가 다 달라서, 범용성을 위해 공통 언어(텍스트)를 사용하기 때문입니다.

Impeller는 앱 시작 전이나 위젯이 처음 그려질 때 셰이더를 미리 컴파일(번역)해두고, 
앱을 사용할 때 번역된 결과물만 읽으면 됨

---

코드를 픽셀로 바뀌주는 렌더링과정에서 
셰이더라는 계산식이 필요한데 이 셰이더를 GPU가 이해할 수 있는 언어로 번역하는 과정이 필요함

스키아
앱이 실행될때 즉 렌더링이 실행중일 때 셰이더 번역을 해서 
프레임이 끊기는 현상(Jank)이 발생

임펠러
앱이 실행되기 전 빌드 될 때 미리 셰이더를 번역해두고
앱 실행중에는 번역된 결과물만 읽음
끊김 없이 부드러움



------
- 영광님: CI/CD란 무엇이며, Flutter 프로젝트에서 어떻게 활용할 수 있나요?
- 진님: 의존성 주입(Dependency Injection)이란 무엇이며, 왜 사용하나요?
- 동준님: Flutter의 Impeller 렌더링 엔진이란 무엇이며, 기존 Skia와 어떤 차이가 있나요?
- 혜림님: Flutter에서 테스트의 종류(Unit, Widget, Integration)와 각각의 차이점에 대해 설명하세요.


---

안드로이드

플러터
버전마다 무엇이 적용이 되었는지
