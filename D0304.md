# RepaintBoundary란?

앱에서
1. 특정 부분이 자주 업데이트되는 위젯 (혼자바쁜 위젯 [애니메이션, 로딩바])
2. 매우 복잡해서 렌더링 비용이 큰 위젯 (가만히 무거운위젯 [고해상도이미지])

해당 위젯 분리하여 불필요한 다시 그리기 작업을 방지하는 최적화 도구입니다.

부모위젯이 스크롤되거나 주변요소가 애니메이션 될 때, 같은 레이어에 있는 자식위젯들이 모두 다시 그려지는데, RepaintBoundary 는 다시 그려지는 것을 차단하여 필요한 부분만 다시 그리도록 "경계"를 설정


## 작동원리
부모 자식 위젯간의 연결을 분리 -> 부모자식 사이에 새로운 레이어, 부모자식간의 다시그리라는 신호를 차단, 다시그리기 신호가 부모로 전파되는 것을 막음(가장 가까운 RepaintBoundary에서 멈춤) 
레스터화 캐싱
- 레이어 분리: 자식 위젯을 위한 새로운 레이어 생성 -> 부모자식간의 연결 분리
- 전파 차단: 다시 그리기 신호가 부모로 전파되는 것을 막음(가장 가까운 RepaintBoundary에서 멈춤) 
- 레스터화 캐싱: 레스터화: 사진을 찍어놓는것과 비슷, 캐싱: 사진첩

RepaintBoundary를 쓰는 순간 **메모리(RAM)**를 추가로 사용하게 되므로 남발하지 말아야 함



"그리기가 너무 복잡하거나", "혼자 너무 자주 변하는" 녀석들에게만 이 방을 만들어주는 것이 정석

다시 그려야하는 영역(혼자 바쁜 위젯)과 다시 그릴필요없는 영역(무거운 위젯) 사이에 벽을 세우는 것

---
어떤 상황에서 사용하는지 정리

1. 스크롤 성능 최적화 (가장 흔한 경우)
리스트뷰(ListView) 안에 복잡한 UI가 들어있을 때 사용합니다.

상황: 리스트를 스크롤하면 부모인 ListView가 움직이죠? 이때 자식 위젯들도 위치가 변하므로 이론적으로는 매 프레임 다시 그려져야 합니다.

사용법: 리스트의 각 아이템 중 **그리기가 복잡한 녀석(이미지 필터, 복잡한 커스텀 페인트 등)**을 RepaintBoundary로 감쌉니다.

효과: 엔진이 아이템을 한 번만 그려서 **사진(캐시)**으로 저장합니다. 스크롤할 때마다 새로 그리는 게 아니라, 저장된 사진 위치만 옮겨서 보여주므로 스크롤이 비단결처럼 부드러워집니다.

2. 국소적인 애니메이션 격리 (민폐 방지)
화면 전체 중 아주 일부분만 계속 변하는 상황입니다.

상황: 화면에 고해상도 지도가 깔려 있고, 그 위에 '내 위치'를 나타내는 작은 점이 계속 깜빡거립니다.

사용법: 깜빡거리는 **작은 점(애니메이션 위젯)**만 RepaintBoundary로 감쌉니다.

효과: 점이 깜빡거릴 때마다 발생하는 '다시 그려!' 신호가 담장에 막힙니다. 뒤에 있는 무거운 지도는 다시 그려지지 않고 엔진이 찍어둔 사진을 그대로 유지합니다.

3. 복잡한 커스텀 그래픽 보존 (자기 방어)
CustomPaint를 사용해 수천 개의 선과 점을 그리는 경우입니다.

상황: 주식 차트나 세밀한 그래프 위젯이 있습니다. 이 위젯 옆에서 다른 위젯(예: 텍스트 필드)이 변해서 화면 전체가 리페인트되는 상황입니다.

사용법: 복잡한 차트 위젯을 RepaintBoundary로 감쌉니다.

효과: 주변에서 무슨 난리가 나든, 차트는 이미 구워진 **이미지(래스터화 캐시)**를 재사용합니다. 수천 번의 수학 연산을 다시 할 필요가 없어지는 것이죠.