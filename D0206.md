# Clean Architecture를 사용하는 이유는 무엇인가요?

1. 변동성이 낮은 영역: 핵심 비즈니스 로직 (High Stability)
원의 중심부인 **엔티티(Entities)**와 **유스케이스(Use Cases)**는 비즈니스의 본질입니다.

특징: 은행 앱이라면 '송금'이나 '이자 계산' 규칙은 기술이 바뀌어도 변하지 않습니다.

클린 아키텍처의 전략: 이 영역을 가장 안쪽에 배치하여 외부의 어떤 변화(DB 교체, UI 변경)에도 흔들리지 않게 보호합니다.

2. 변동성이 높은 영역: 세부사항 (High Volatility)
원의 바깥쪽인 프레임워크, DB, UI, 외부 라이브러리 등은 언제든 바뀔 수 있는 것들입니다.

특징: "React에서 Vue로 바꿀까?", "MySQL 말고 MongoDB를 써볼까?" 같은 고민들이 발생하는 영역입니다.

클린 아키텍처의 전략: 이들을 '플러그인'처럼 취급합니다. 언제든 갈아 끼울 수 있도록 인터페이스 뒤로 숨깁니다.

3. 변동성을 다루는 핵심 메커니즘
① 의존성 방향의 제어
변동성이 높은 것(바깥쪽)이 변동성이 낮은 것(안쪽)에 의존하게 만듭니다.

안정된 방향으로의 의존: 변하기 쉬운 코드는 변하지 않는 코드에 의존해도 되지만, 그 반대는 안 됩니다. 그래야 외부가 변할 때 내부 핵심 로직이 무너지지 않습니다.

② 추상화라는 방어벽
변동성이 큰 외부 모듈과 내부 로직 사이에 **인터페이스(Interface)**를 둡니다.

내부 로직은 "데이터가 어떻게 저장되는지(SQL인지 NoSQL인지)" 몰라도 됩니다.

단지 "데이터를 저장해줘"라는 추상적인 명령만 내립니다. 실제 구현은 변동성이 높은 바깥쪽 계층에서 담당합니다.

4. 변동성 관리의 이점 (Business Value)
결정의 지연 (Defer Decisions): 프로젝트 초기, 어떤 DB를 쓸지 결정하지 못했어도 비즈니스 로직을 먼저 개발할 수 있습니다. 세부사항(변동성 큰 것)은 나중에 결정해도 되기 때문입니다.

유지보수 비용 절감: 최신 트렌드에 따라 UI를 갈아엎더라도, 우리 회사의 핵심 자산인 비즈니스 로직은 단 한 줄도 고칠 필요가 없습니다.

💡 정리하자면
클린 아키텍처는 **"변동성이 적은 핵심 로직을 변동성이 큰 주변 환경으로부터 독립시켜서, 변화에 유연하게 대응하는 구조"**를 만드는 것입니다.