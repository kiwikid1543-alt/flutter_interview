값 형식과 참조 형식의 차이에 대해 설명하세요. (메모리에 대해서)

## 값 형식
c c++ 자바 

c언어 포인터
포인터의 값형식과 참조형식

넓게 아는것이 중요!


- 변수가 데이터의 실제 값을 직접 저장
- int,float,double,bool,struct,enum --> 


- 값 형식은 주로 스택 메모리 영역에 저장

- 변수가 선언된 블록이 종료되면 메모리에서 즉시 제거

- 값을 복사 할 때 데이터 전체가 새로 생성됨

- 함수 안에서 사용한 변수가 밖으로 빠져나올 수 없음

## 참조 형식
- 변수가 데이터가 위치해 있는 메모리의 주소를 저장
- class, interface, delegate, array, string
- 실제 데이터는 힙에 메모리영역에 저장되고 주소값은 스택에 저장

- 여러 변수가 동일한 주소를 가리킬 수 있고, 
- 힙에 있는 데이터는 더이상 참조되지 않을 때 가비지 컬렉터에 의해 제거됨

- 힙은 개발자가 직접 해제하거나 가비지컬렉터가 직접 치워줘야되는 영역 -> 참조형식이 많으면 메모리누수 발생가능

- 복사본을 수정하면 동일한 주소를 가리키는 원본 데이터도 함께 변한 것처럼 보임


---
파이어베이스 펑션즈