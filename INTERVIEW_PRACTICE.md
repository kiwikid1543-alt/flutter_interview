## 1. Flutter 앱 개발에서 ‘상태’란 무엇을 의미하나요?
플러터에서 **상태(State)**란 **'위젯이 빌드되는 시점에 읽을 수 있고, 위젯의 수명 주기(Lifecycle) 동안 변할 수 있는 데이터'**를 의미합니다.
플러터는 **'UI = f(State)'**라는 공식을 따르는 선언형 UI 프레임워크입니다. 즉, 상태가 변경되면 프레임워크가 이를 감지하여 build() 메서드를 다시 실행하고, 변경된 상태를 바탕으로 화면을 새로 고침(Rebuild)하게 됩니다.
관리 범위에 따라서는 해당 위젯 내에서만 쓰이는 **단기 상태(Ephemeral State)**와, 앱 전체나 여러 페이지에서 공유되는 **앱 상태(App State)**로 나누어 관리할 수 있습니다.

위젯: 앱의 화면을 구성하는 모든 시각적 요소와 논리적 구조를 정의하는 최소단위이자 설계도
빌드: 개발자가 작성한 위젯 설계도를 바탕으로 플러터 엔진이 화면에 그릴 실체(Element)를 생성하거나 업데이트하는 화면 구성 과정을 의미합니다



## 2. ListView, ListView.builder, SingleChildScrollView + Column 3가지의 차이점은 무엇인가요?
SingleChildScrollView + Column: 내부의 모든 위젯을 한 번에(All-at-once) 메모리에 올립니다. 데이터가 적고 항목이 정해져 있는 '설정 화면'이나 '입력 폼'에 적합합니다.
ListView (기본 생성자): Column과 비슷하게 모든 자식 위젯을 미리 생성하지만, 리스트 전용 기능을 제공합니다. 역시 데이터가 적을 때 사용합니다.
ListView.builder: 가장 중요한 차이점은 **'지연 로딩(Lazy Loading)'**입니다. 화면에 보이는 부분과 그 주변의 위젯만 동적으로 생성하고, 화면에서 사라진 위젯은 메모리에서 해제하거나 재사용합니다. 따라서 서버에서 받아오는 수천 개의 데이터나 끝을 알 수 없는 피드를 보여줄 때 반드시 사용해야 하는 방식입니다.

## 3. Flutter에서 상수 클래스란 무엇이며 왜 사용하나요?
상수 클래스란 앱 전체에서 공통으로 사용되는 변하지 않는 값들(색상, 폰트 크기, API 주소, 여백 등)을 한곳에 모아 관리하는 클래스를 의미합니다. 이를 사용하는 이유는 크게 세 가지입니다.
유지보수의 용이성: 예를 들어, 앱의 주색상(Primary Color)을 바꾼다고 할 때, 모든 파일의 색상 코드를 수정하는 대신 상수 클래스의 값 하나만 바꾸면 전체에 적용됩니다.
재사용성과 가독성: 0xFF4285F4라는 숫자보다 AppColors.googleBlue라는 이름을 쓰는 것이 코드를 이해하기 훨씬 쉽습니다.
컴파일 최적화: 플러터에서는 const 키워드를 사용하여 상수를 선언하면, 앱이 실행될 때 매번 새로 만드는 것이 아니라 메모리의 한 공간에 고정해두고 재사용합니다. 이는 불필요한 **리빌드(Rebuild)**를 방지하여 성능 향상에 도움을 줍니다.

## 4. Riverpod 상태관리 패턴의 특징은 무엇인가요?
Riverpod은 플러터의 기본 상태 관리 방식인 Provider를 만든 개발자가 그 한계를 극복하기 위해 만든 라이브러리입니다. 제가 생각하는 Riverpod의 핵심 특징은 크게 세 가지입니다.
컴파일 타임 안전성: 가장 큰 장점은 컴파일 시점에 에러를 잡아낼 수 있다는 점입니다. 기존 Provider는 런타임에 위젯 트리에서 해당 데이터를 찾지 못하면 앱이 튕기는(ProviderNotFoundException) 문제가 있었지만, Riverpod은 전역적으로 선언되므로 이런 설계상의 실수를 사전에 방지합니다.
위젯 트리와의 독립성: Riverpod은 상태를 위젯 트리 외부에 선언합니다. 따라서 특정 위젯 아래에 있어야만 데이터를 읽을 수 있는 제약이 사라져, 코드 구조가 훨씬 자유롭고 테스트하기가 쉬워집니다.
다양한 프로바이더 제공: 단순히 데이터만 보관하는 것이 아니라, 비동기 데이터를 처리하는 FutureProvider, 스트림을 다루는 StreamProvider 등 상황에 맞는 최적화된 도구를 제공하여 보일러플레이트 코드를 획기적으로 줄여줍니다.

## 5. Riverpod 상태관리와 provider 상태관리의 차이는 무엇인가요?
위젯 트리 의존성 해결: Provider는 위젯 트리 안에 존재해야 하므로, 위젯 트리 외부에서 접근하려면 에러가 발생하기 쉽습니다. 반면 Riverpod은 상태를 **전역(Global)**으로 선언하되 ProviderScope 내에서 관리하므로 위젯 트리 구조에 제약을 받지 않습니다.
런타임 에러 방지: Provider를 쓸 때 가장 흔히 겪는 에러가 'ProviderNotFoundException'입니다. Riverpod은 전역 선언 방식을 통해 이를 컴파일 타임에 해결하여, 존재하지 않는 상태에 접근하는 실수를 원천 봉쇄합니다.
동일한 타입의 다중 선언: Provider는 같은 타입의 데이터(예: String 두 개)를 위젯 트리에 넣으면 관리가 까다롭지만, Riverpod은 변수명으로 구분되기 때문에 동일한 타입의 프로바이더를 여러 개 선언하고 사용하는 것이 매우 자유롭습니다."

## 6. ViewModel의 역할은 무엇인가요?
ViewModel은 한마디로 'View(UI)와 Model(데이터) 사이의 중개자' 역할을 합니다. 구체적인 역할은 다음과 같습니다.
비즈니스 로직 분리: 화면에 버튼을 배치하는 것과 버튼을 눌렀을 때 데이터를 계산하거나 서버에 요청하는 것은 별개의 일입니다. ViewModel은 이런 **로직(Logic)**을 전담하여 View 코드를 가볍고 간결하게 유지해줍니다.
상태 관리 및 데이터 가공: 데이터베이스나 서버에서 받아온 가공되지 않은 데이터를 View가 바로 보여주기 편한 형태(상태)로 변환하여 보관합니다.
테스트 용이성: UI가 없어도 로직이 정상적으로 작동하는지 **유닛 테스트(Unit Test)**를 할 수 있게 해줍니다. 즉, 화면을 직접 눌러보지 않고도 코드만으로 검증이 가능해집니다.

7. MVVM 패턴이란 무엇인가요?
MVVM은 Model, View, ViewModel의 약자로, 앱의 구성 요소를 세 가지 역할로 엄격히 나누는 아키텍처 패턴입니다.
Model (모델): 앱에서 사용하는 실제 데이터와 그 데이터를 처리하는 비즈니스 로직을 담습니다. (예: 데이터베이스, API 응답 객체)
View (뷰): 사용자에게 보여지는 화면(UI)입니다. 플러터에서는 위젯들이 이에 해당하며, 최대한 로직을 배제하고 **'상태를 보여주는 역할'**에만 집중합니다.
ViewModel (뷰모델): View가 필요로 하는 데이터를 Model로부터 받아와서 **'View가 쓰기 좋은 상태'**로 가공합니다. 또한 View에서 발생하는 이벤트를 받아 로직을 수행합니다.
플러터는 선언형 UI 프레임워크이기 때문에, ViewModel이 상태를 변경하면 View가 자동으로 리빌드되는 MVVM 패턴과 궁합이 매우 잘 맞습니다. 이를 통해 코드의 가독성을 높이고 테스트가 용이한 구조를 만들 수 있습니다."

## 8. MVVM 패턴의 특징과 장점은 무엇인가요?
"MVVM 패턴의 가장 큰 특징은 **'View와 Model 사이의 의존성을 완전히 제거했다'**는 점입니다. 이를 통해 얻을 수 있는 장점은 다음과 같습니다.
유지보수의 독립성: View는 오직 ViewModel만 바라보고, Model은 ViewModel도 모른 채 데이터에만 집중합니다. 따라서 UI 디자인을 전면 수정하더라도 데이터 로직을 건드릴 필요가 없고, 반대로 서버 데이터 구조가 바뀌어도 UI 코드를 일일이 수정하지 않아도 됩니다.
데이터 바인딩을 통한 자동화: ViewModel의 상태가 업데이트되면 View가 이를 감지하여 자동으로 화면을 갱신합니다. 이는 개발자가 일일이 '화면을 그려라'라고 명령하는 코드를 줄여주어 실수를 방지합니다.
병렬 개발 가능: 디자이너와 UI 개발자가 View를 만드는 동안, 로직 개발자는 ViewModel과 Model을 동시에 개발할 수 있습니다. 서로 간의 간섭이 적기 때문에 협업 효율이 극대화됩니다.
테스트 효율성: View는 복잡한 UI 환경(시뮬레이터, 디바이스)이 필요하지만, ViewModel은 순수 코드로만 이루어져 있어 유닛 테스트가 매우 빠르고 간편합니다."

## 9. MVVM 패턴과 StatefulWidget의 차이점을 설명하세요.
StatefulWidget과 MVVM 패턴의 가장 큰 차이점은 '관심사의 분리(Separation of Concerns)' 여부입니다.
StatefulWidget (밀결합 구조):
하나의 클래스 안에 **UI 코드(Widget)**와 **상태 관리 로직(State)**이 함께 존재합니다.
작고 단순한 기능을 구현할 때는 빠르고 직관적이지만, 로직이 복잡해지면 코드의 길이가 수백 줄로 늘어나 가독성이 떨어지고 유지보수가 힘들어집니다.
UI와 로직이 섞여 있어, 화면을 직접 띄우지 않고는 로직만 따로 테스트하기 어렵습니다.
MVVM 패턴 (약결합 구조):
UI는 View에, 로직은 ViewModel에 엄격히 나누어 작성합니다.
ViewModel은 플러터 위젯에 의존하지 않는 순수 다트(Dart) 코드로 작성될 수 있어, UI 없이도 비즈니스 로직을 검증하는 유닛 테스트가 가능합니다.
상태 관리 도구(Riverpod 등)를 함께 사용하여, 여러 위젯이 동일한 ViewModel의 상태를 공유하거나 관찰하기에 훨씬 유리합니다.
결론적으로, StatefulWidget은 단기적이고 지엽적인 UI 상태를 관리하기에 적합하고, MVVM은 앱의 핵심 비즈니스 로직과 복잡한 상태를 체계적으로 관리하기 위해 도입하는 전략이라고 할 수 있습니다."

## 10. Flutter에서 위젯트리란 무엇인가요?
**위젯 트리(Widget Tree)**란 플러터 UI를 구성하는 위젯들의 부모-자식 관계를 나타내는 계층 구조를 의미합니다.
계층적 구조: 최상단의 MaterialApp부터 시작해서 Scaffold, Center, Text와 같은 세부 위젯들이 나무 가지처럼 뻗어 나가는 형태입니다.
UI 정의서: 위젯 트리는 현재 화면에 무엇이 보여야 하는지 정의하는 설계도 역할을 합니다. 플러터는 이 트리를 바탕으로 실제 화면을 렌더링합니다.
효율적인 업데이트: 상태가 변경되어 화면을 다시 그려야 할 때, 플러터는 전체 트리를 새로 만드는 대신 **트리를 비교(Diffing)**하여 바뀐 부분만 효율적으로 찾아내 업데이트합니다.
추가로, 실제 내부적으로는 위젯 트리 외에도 성능 최적화를 위한 **엘리먼트 트리(Element Tree)**와 **렌더 객체 트리(RenderObject Tree)**가 함께 동작하며 화면을 완성하게 됩니다."



## 11. 위젯이 화면에 그려지기까지의 과정을 설명해보세요.
플러터에서 위젯이 화면에 그려지기까지는 크게 3가지 트리 구조가 협력하는 과정을 거칩니다.
위젯 트리 (Widget Tree): 개발자가 작성한 코드로, 화면의 구성을 정의하는 **'설계도'**입니다. 매우 가볍고 상태가 바뀔 때마다 빈번하게 새로 생성됩니다.
엘리먼트 트리 (Element Tree): 위젯과 렌더 객체를 연결하는 **'중개자'**입니다. 위젯 트리가 새로 생성되어도 엘리먼트는 기존 정보를 유지하며, 바뀐 부분만 찾아내어 실제 그리는 도구(RenderObject)에 전달합니다. (부모-자식 관계의 '뼈대' 역할을 합니다.)
렌더 객체 트리 (RenderObject Tree): 실제로 화면의 크기를 계산(Layout)하고 색칠(Paint)하는 **'실무자'**입니다. 생성 비용이 비싸기 때문에 엘리먼트 트리의 지시에 따라 꼭 필요한 부분만 업데이트됩니다.
결과적으로 '설계도(위젯)가 들어오면 -> 중개자(엘리먼트)가 변경 사항을 확인하고 -> 실무자(렌더 객체)에게 명령하여 화면을 그리는' 순서로 동작합니다."

## 12. Flutter에서 BuildContext 란 무엇인지 예시를 들어 설명해보세요.
"BuildContext는 한마디로 '현재 위젯이 위젯 트리상에서 어디에 위치하고 있는지'를 알려주는 정보 객체입니다.
위치 정보 (Location): 모든 위젯은 자신만의 context를 가집니다. 이는 "나는 지금 전체 트리 중에서 어떤 부모 밑에 있고, 어떤 자식을 가지고 있어"라는 주소록 같은 역할을 합니다.
트리 탐색 (Tree Traversal): 부모 위젯의 정보(예: 테마, 미디어 쿼리 등)를 찾고 싶을 때 context를 사용합니다. 예를 들어 Theme.of(context)는 "내 위치(context)에서부터 위로 올라가며 가장 가까운 테마 정보를 찾아줘"라는 뜻입니다.
서비스 연결: 화면 이동(Navigator)이나 스낵바(ScaffoldMessenger)를 띄울 때도 현재 어느 화면에서 이 동작이 일어나는지 알기 위해 context가 필수적으로 사용됩니다.

==========================================================

## 1. 애니메이션 속성 중 curve 와 duration 에 대해 설명하세요.
Flutter에서 애니메이션은 특정 값(예: 크기, 색상, 위치)이 시간에 따라 변화하는 과정입니다. 이때 Duration은 '얼마나 오래' 실행될지를, Curve는 '어떤 속도감으로' 실행될지를 결정합니다.
1. Duration (지속 시간)Duration은 애니메이션이 시작부터 끝까지 걸리는 전체 시간을 정의합니다.단위: milliseconds, seconds 등을 사용하여 설정합니다.특징: * 너무 짧으면 사용자가 변화를 인지하기 어렵고(부자연스러운 깜빡임), 너무 길면 앱이 느리다는 인상을 줍니다.일반적으로 마이크로 인터랙션은 200ms~300ms, 화면 전환은 400ms~500ms 정도가 사용자 경험(UX) 측면에서 가장 쾌적하다고 알려져 있습니다.
2. Curve (변화율/곡선)Curve는 시간이 흐름에 따라 값이 변화하는 속도의 비율을 정의합니다. 수학적으로는 $x$축을 시간, $y$축을 변화량으로 하는 함수입니다.Linear (선형): 처음부터 끝까지 일정한 속도로 움직입니다. 기계적이고 딱딱한 느낌을 줄 수 있습니다.Curves.easeIn: 천천히 시작해서 끝날 때 속도가 붙습니다.Curves.easeOut: 빠르게 시작해서 끝날 때 부드럽게 멈춥니다. (사용자가 가장 편안함을 느끼는 곡선 중 하나입니다.)Curves.elasticIn/Out: 마치 용수철처럼 튕기는 물리적인 효과를 줍니다.


## 2. 암시적 애니메이션과 명시적 애니메이션의 차이는 무엇인가요?
이 둘의 가장 큰 차이는 **"제어의 주도권이 누구에게 있는가"**와 **"애니메이션의 복잡도"**에 있습니다.
1. 암시적 애니메이션 (Implicit Animations)
"값이 바뀌면 애니메이션을 알아서 처리해라"라고 선언하는 방식입니다.
특징: Flutter 프레임워크가 애니메이션의 시작과 끝, 그리고 관리를 모두 담당합니다. 개발자는 단순히 **타겟 값(Target Value)**만 바꿔주면 됩니다.
주요 위젯: AnimatedContainer, AnimatedOpacity, AnimatedPositioned 등 'Animated' 접두사가 붙은 위젯들이 해당합니다.
장점: 구현이 매우 쉽고 코드가 간결합니다. 별도의 Controller가 필요 없습니다.
단점: 애니메이션을 중간에 멈추거나, 역재생하거나, 무한 반복시키는 등의 세밀한 제어가 불가능합니다.
2. 명시적 애니메이션 (Explicit Animations)
"내가 직접 애니메이션의 모든 순간을 통제하겠다"라는 방식입니다.
특징: 개발자가 **AnimationController**를 직접 생성하고 관리해야 합니다. Ticker를 통해 화면이 새로고침될 때마다 애니메이션의 진행 상태를 명시적으로 지시합니다.
주요 위젯: RotationTransition, ScaleTransition, SizeTransition 등 'Transition' 접미사가 붙은 위젯들이나 AnimatedBuilder, AnimatedWidget을 사용합니다.
장점: 강력한 제어권을 가집니다. (시작, 정지, 반복, 역재생, 특정 지점으로 이동 등)
단점: AnimationController를 dispose 해줘야 하는 등 생명주기 관리가 필요하며, 코드가 상대적으로 복잡해집니다.


"사용자의 입력(setState)으로 인해 UI의 속성(값)이 바뀌는데, 그 과정이 딱딱하지 않고 부드러웠으면 좋겠다!" 이 마음이 들 때 가장 먼저 암시적 위젯(Animated...)을 떠올리시면 됩니다. 
"나는 이 애니메이션을 멈추고 싶을 때 멈추고, 거꾸로 돌리고 싶을 때 돌리고, 혹은 앱이 켜져 있는 동안 영원히 돌리고 싶다!" 라는 욕구가 생기면 무조건 명시적(AnimationController)으로 가야 합니다.
- 암시적 애니메이션은 상태 값만 바꾸면 프레임워크가 알아서 중간 과정을 채워주는 '선언형' 방식으로, 단순한 수치 변화에 효율적입니다.
-명시적 애니메이션은 컨트롤러를 통해 재생, 정지, 반복을 직접 지휘하는 '명령형' 방식으로, 복잡하고 정교한 제어가 필요할 때 사용합니다.


암시적 플러터가알아서 무슨 값: 
명시적 컨트롤러 조정, 

## 3. Flutter에서 스로틀링은 무엇인가요? 예를 들어 설명하세요.
**스로틀링(Throttling)**은 짧은 시간 동안 반복되는 이벤트를 제어하여 시스템의 부하를 줄이고 성능을 최적화하는 기법입니다.
스로틀링은 **"이벤트가 아무리 많이 발생해도, 일정 시간 간격(Interval) 내에는 최대 한 번만 실행"**되도록 제한하는 것을 의미합니다.
동작 원리: '이벤트'가 발생하면 즉시 '실행'(또는 설정에 따라 지연 실행)하고, 이후 정해진 '시간'(예: 1초) 동안 발생하는 동일한 이벤트는 모두 무시합니다. 시간이 지나면 다시 이벤트를 받을 준비를 합니다.

## 4. Flutter에서 디바운싱은 무엇인가요? 예를 들어 설명하세요.
디바운싱은 **"연속으로 발생하는 이벤트를 하나로 그룹화하여, 마지막 '이벤트'가 끝난 후 일정 '시간'이 지나면 단 한 번만 '실행'"**하게 만드는 기술입니다.
동작 방식: 이벤트 발생 -> 타이머 시작 -> 타이머가 끝나기 전 또 이벤트 발생 -> 기존 타이머 취소 및 새로 시작 -> 타이머 만료 시점에 최종 실행.
예시) 엘리베이터 문이 닫히려고 할 때 누군가 '열림' 버튼을 누르면, 닫히기까지의 대기 시간이 초기화됩니다.
사람들이 계속해서 버튼을 누르는 동안 문은 절대 닫히지 않습니다.
마지막 사람이 버튼을 누르고 나서 아무도 버튼을 누르지 않은 채 5초가 지나야 비로소 문이 닫히고 이동합니다.

## 5. Flutter에서 무한 스크롤을 구현하는 방법은 무엇인가요?
ScrollController를 통해 스크롤 위치를 감지하고, 바닥에 닿기 직전에 데이터를 추가로 불러오는 로직을 실행하는 것입니다.
1. ScrollController를 이용한 기본 구현 (Step-by-Step)
가장 보편적인 구현 순서는 다음과 같습니다.
ScrollController 생성: StatefulWidget 내에서 컨트롤러를 선언합니다.
리스너 등록: initState에서 컨트롤러에 리스너를 추가하여 스크롤 위치를 감시합니다.
임계값(Threshold) 확인: 사용자가 리스트의 끝(또는 끝 근처)에 도달했는지 계산합니다.
데이터 로드 및 상태 업데이트: 새로운 데이터를 API로 호출하고, setState나 상태 관리 라이브러리를 통해 리스트를 갱신합니다.

2. UI 구성: ListView.builder 활용
무한 스크롤은 데이터가 수백, 수천 개가 될 수 있으므로 반드시 **ListView.builder**를 사용해야 합니다.
메모리 최적화: 화면에 보이는 요소만 렌더링하므로 성능 저하를 방지합니다.
로딩 인디케이터: 리스트의 가장 마지막 아이템(index == data.length) 자리에 CircularProgressIndicator를 배치하여 사용자에게 로딩 중임을 알립니다.

## 6. Flutter에서 당겨서 새로고침 기능을 구현하는 방법은 무엇인가요?
RefreshIndicator 위젯으로 리스트를 감싸고, 사용자가 화면을 아래로 당겼을 때 실행될 비동기 함수를 onRefresh 콜백에 연결하여 구현합니다. 데이터 로딩이 완료될 때까지 상단에 로딩 아이콘이 표시되며, 내부적으로는 Future가 완료되는 시점에 인디케이터가 자동으로 사라집니다.
아이템 개수가 적어 스크롤이 생기지 않는 경우에는 AlwaysScrollableScrollPhysics를 설정하여 강제로 동작하게 만드는 것이 실무적인 팁입니다.

## 7. 디자이너와의 협업 과정에서 테마 커스터마이징은 활용한 적이 있나요? 있다면 예시를 들어 설명하세요.
네, ThemeData를 활용해 앱 전체의 브랜드 아이덴티티를 일관되게 관리한 경험이 있습니다. 디자이너가 정의한 **디자인 시스템(Color Palette, Typography)**을 Theme의 각 속성에 미리 정의해두면, 개별 위젯에서 매번 스타일을 지정할 필요 없이 공통된 UI 톤을 유지할 수 있습니다.ㅈㅇ
실제로 다크 모드와 라이트 모드 전환 기능을 구현할 때, colorScheme의 primary와 surface 값을 커스텀하여 테마 변경 시 모든 화면이 즉각적으로 대응하도록 최적화한 적이 있습니다.

## 8. Flutter에서 네이티브 기능과 연결하기 위해 패키지를 사용한 경험이 있나요? 있다면 예시를 들어 설명하세요.
image_picker나 geolocator 같은 패키지를 사용하여 카메라 접근 및 GPS 위치 정보 기능을 구현한 경험이 있습니다. 패키지 내부적으로는 플랫폼 채널을 통해 네이티브 API와 통신하므로, 복잡한 네이티브 코드 작성 없이도 하드웨어 기능을 손쉽게 제어할 수 있었습니다.

## 9. StatefulWidget의 생명주기에 대해 설명하세요.
초기화 단계: 위젯이 생성될 때 createState()가 호출되고, 상태 객체가 생성되면 딱 한 번 initState()가 실행되어 초기 로직이나 리스너를 등록합니다.
업데이트 단계: 부모 위젯의 변경(didUpdateWidget)이나 자체적인 setState() 호출에 의해 build()가 재실행되며 화면을 갱신합니다.
해제 단계: 위젯이 트리에서 완전히 제거될 때 dispose()가 호출되며, 여기서 애니메이션 컨트롤러나 스트림 등 사용하던 리소스를 반드시 해제해야 메모리 누수를 방지할 수 있습니다.

## 10. StatefulWidget에서 didUpdateWidget은 어떤 상황에서 사용하나요?
didUpdateWidget**은 부모 위젯에 의해 위젯의 설정(Configuration)이 변경되었지만, State 객체는 유지해야 할 때 사용합니다.
구체적으로는 다음과 같은 상황에서 주로 활용합니다.
애니메이션 재설정: 부모로부터 전달받은 duration이나 curve 값이 바뀔 때, 기존 AnimationController의 속성을 업데이트하거나 애니메이션을 다시 시작해야 하는 경우입니다.
데이터 동기화: 위젯에 주입된 ID나 파라미터가 변경되어, 새로운 데이터에 맞춰 내부 스트림을 다시 구독(Resubscribe)하거나 리소스를 재할당해야 할 때 사용합니다.
속성 비교: 메서드의 인자로 들어오는 oldWidget과 현재의 widget을 비교하여, 특정 필드가 변했을 때만 무거운 연산을 수행하도록 최적화할 수 있습니다.

## 11. Flutter에서 Isolate란 무엇인가요?
Isolate는 Dart 언어의 독립적인 실행 단위로, 각자 전용 메모리(Heap)를 소유하며 다른 Isolate와 메모리를 공유하지 않는다는 점이 가장 큰 특징입니다.
일반적인 스레드와 달리 공유 메모리가 없으므로 데이터 경합(Race Condition)이나 락(Lock) 문제가 발생하지 않으며, 오직 '메시지 패싱'을 통해서만 통신합니다.
실무에서는 이미지 가공이나 대규모 데이터 파싱처럼 CPU 부하가 큰 작업을 별도의 Isolate에서 수행하여, UI를 담당하는 메인 Isolate가 끊김(Jank) 없이 매끄럽게 동작하도록 최적화하는 데 사용합니다.

## 12. Clean Architecture란 무엇인가요? Flutter에서 사용할 수 있는 예시와 함께 설명하세요.
**클린 아키텍처(Clean Architecture)**는 소프트웨어의 관심사를 계층별로 분리하여, 외부 라이브러리나 UI의 변화에 비즈니스 로직이 영향을 받지 않도록 설계하는 원칙입니다.
사용자 로그인 기능을 구현할 때, UI는 LoginUseCase만 호출하고, 이 UseCase는 내부적으로 AuthRepository 인터페이스를 통해 실제 서버 통신을 수행하게 함으로써 **테스트와 유지보수**가 용이한 구조를 만듭니다.

## 13. Clean Architecture를 사용하는 이유는 무엇인가요?
코드의 결합도를 낮추고 유지보수성을 극대화
관심사 분리(Separation of Concerns): 비즈니스 로직(Domain)이 UI(Presentation)나 데이터베이스(Data) 같은 외부 환경에 의존하지 않으므로, 프레임워크나 라이브러리를 교체하더라도 핵심 로직은 수정할 필요가 없습니다.
테스트 용이성: 각 계층이 독립적이기 때문에, UI 없이도 순수한 비즈니스 로직만 따로 떼어 유닛 테스트(Unit Test)를 수행하기 매우 유리합니다.
병렬 개발 및 확장성: 여러 개발자가 계층별로 나누어 작업할 수 있으며, 프로젝트 규모가 커져도 코드의 구조가 명확하여 기능 추가나 변경 시 발생하는 부작용(Side Effect)을 최소화할 수 있습니다.

---


## 14. Flutter에서 비동기 처리를 하는 방법에 대해 설명해보세요.
비동기 처리는 주로 **Future와 Stream**을 통해 이루어지며, 이를 더 직관적으로 다루기 위해 async/await 키워드를 사용합니다.
Future: 데이터 파일 읽기나 API 호출처럼 **단 한 번의 작업 완료를 기다릴 때 사용하며,** await를 통해 결과가 올 때까지 비동기적으로 대기합니다.
Stream: 채팅 메시지나 위치 정보 업데이트처럼 **지속적인 데이터의 흐름을 처리할 때 사용하며,** listen이나 StreamBuilder를 통해 실시간 변화를 감지합니다.
실무적인 팁: UI 단에서는 FutureBuilder나 StreamBuilder를 활용하여 데이터의 로딩, 성공, 에러 상태에 따른 화면을 선언적으로 구현하는 것이 일반적입니다.

## 15. Isolate와 스레드의 차이는 무엇인가요? -나-
일반적인 **스레드는 같은 프로세스 내에서 메모리(Heap)를 공유**하기 때문에 데이터 경합을 막기 위한 락(Lock)이나 뮤텍스(Mutex) 처리가 필수적이지만, **Isolate는 각자 독립적인 메모리 공간을 가집니다.** 따라서 Isolate끼리는 서로의 메모리에 직접 접근할 수 없고, 오직 **'메시지 패싱' 방식을 통해서만 데이터를 주고받기 때문에 교착 상태(Deadlock)** 같은 복잡한 멀티스레딩 이슈에서 자유롭습니다.

## 16. Isolate와 Future의 차이는 무엇인가요? -나-
Future는 메인 Isolate 내의 작업 대기열(Event Loop)에서 순서를 기다리는 방식인 반면, Isolate는 별도의 메모리와 CPU 제어권을 가진 독립적인 실행 흐름이라는 점이 가장 큰 차이입니다.
Future는 작업이 완료될 때까지 잠시 미뤄두는 '비동기 예약'이므로 무거운 연산을 수행하면 UI가 멈출 수 있지만, Isolate는 물리적으로 다른 스레드에서 돌아가므로 아무리 무거운 연산을 해도 메인 UI 흐름에 영향을 주지 않습니다.
따라서 단순한 API 호출이나 파일 읽기는 Future를 사용하고, 고해상도 이미지 처리나 복잡한 알고리즘 연산은 Isolate를 사용하여 구현합니다.

## 17. 이벤트 루프란 무엇인가요? -나-
**이벤트 루프(Event Loop)**는 큐(Queue)에 쌓인 비동기 작업들을 순차적으로 꺼내어 실행하는 Dart의 단일 스레드 실행 메커니즘입니다.
이벤트 루프는 Microtask Queue(우선순위 높음)와 Event Queue(사용자 입력, I/O 등)를 지속적으로 확인하며, 메인 스레드가 비어 있을 때마다 작업을 하나씩 처리하여 앱이 멈추지 않고 반응성을 유지하게 합니다.

## 18. Flutter에서 AOT와 JIT 컴파일러란 무엇인가요? 어떤 상황에서 주로 사용하나요?
**JIT(Just-In-Time)**는 앱 실행 중에 코드를 실시간으로 컴파일하여 **개발 단계의 '핫 리로드(Hot Reload)'**를 가능하게 하며, 수정 사항을 즉시 화면에 반영해 생산성을 높여줍니다. 반면 **AOT(Ahead-Of-Time)**는 앱 배포 전에 미리 기계어로 컴파일하여 출시용 빌드(Release Build) 시 빠른 실행 속도와 일관된 성능을 보장합니다.
따라서 개발 중에는 JIT의 유연함을 활용해 빠르게 기능을 구현하고, 사용자에게 전달할 때는 AOT의 최적화된 성능을 활용하는 것이 Flutter

## 19. AOT와 JIT 컴파일러는 서로 어떤 차이점이 있는지 설명해보세요.
**JIT(Just-In-Time)**는 프로그램 실행 중에 실시간으로 코드를 기계어로 번역하여 **수정 사항을 즉시 반영(Hot Reload)**할 수 있게 해주지만, 런타임 부하로 인해 실행 속도는 상대적으로 느립니다. 반면 **AOT(Ahead-Of-Time)**는 실행 전 빌드 단계에서 미리 컴파일을 완료하므로, 배포 시 앱 실행 속도가 매우 빠르고 성능이 안정적이라는 차이가 있습니다.
결론적으로 JIT는 개발자의 생산성에, AOT는 사용자의 최종 실행 성능에 최적화된 컴파일 방식입니다.

## 20. StatefulWidget 과 StatelessWidget 의 차이는 무엇인가요?
StatelessWidget은 한 번 화면에 그려지면 내부 상태가 변하지 않는 정적인 위젯으로, 부모로부터 받은 데이터(변수)가 변경되지 않는 한 화면을 다시 그리지 않습니다. 반면 StatefulWidget은 유저의 상호작용이나 데이터 변화에 따라 setState()를 호출하여 동적으로 UI를 갱신할 수 있는 상태 객체(State)를 별도로 가집니다.
단순히 정보를 표시하는 용도라면 메모리 효율이 좋은 StatelessWidget을, 버튼 클릭이나 입력에 따라 화면이 실시간으로 바뀌어야 한다면 StatefulWidget을 선택하는 것이 원칙입니다.

## 21. Flutter에서 무한 스크롤, 당겨서 새로고침 기능을 사용하는 이유는 무엇인가요?
사용자 경험향상과 앱 성능 최적화
성능 최적화: 수천 개의 데이터를 한꺼번에 불러오면 메모리 부하가 심해지고 로딩 시간이 길어집니다. 무한 스크롤은 필요한 만큼만 데이터를 나누어 가져오는 **지연 로딩(Lazy Loading)**을 통해 앱을 가볍게 유지합니다.
사용자 편의성 (UX): 페이지 번호를 누르는 번거로움 없이 스크롤만으로 콘텐츠를 계속 소비하게 하여 체류 시간을 늘립니다. 또한, 당겨서 새로고침은 모바일 환경에서 가장 직관적으로 최신 데이터를 갱신할 수 있는 표준 인터페이스입니다.

## 22. Flutter에서 네이티브 코드와 연동하는 방법에 대해 설명해보세요.
Flutter에서 네이티브 코드(Android의 Java/Kotlin, iOS의 Obj-C/Swift)와 연동하는 가장 일반적인 방법은 **플랫폼 채널(Platform Channels)**을 사용하는 것입니다.
구체적인 연동 방식은 다음과 같습니다.
MethodChannel: Dart에서 특정 메서드를 호출하면 네이티브에서 해당 로직을 실행하고 결과를 반환하는 비동기 함수 호출 방식입니다. 배터리 상태 확인이나 기기 고유 기능을 실행할 때 주로 사용합니다.
EventChannel: 네이티브에서 발생하는 센서 데이터나 네트워크 상태 변화처럼 **지속적인 데이터 흐름(Stream)**을 Flutter로 전달할 때 사용합니다.
BasicMessageChannel: 이미지 데이터나 복잡한 메시지를 인코딩하여 양방향으로 주고받을 때 활용합니다.
실무적인 팁:
네이티브 연동 시 가장 중요한 것은 Dart와 네이티브 간의 **메서드 이름과 데이터 타입(Serialization)**을 정확히 일치시키는 것입니다. 최근에는 Pigeon이라는 패키지를 사용해 타입 안정성이 보장된 코드를 자동 생성하여 실수를 줄이기도 합니다.


1. OS(운영체제)란 무엇인가요?
사용자와 컴퓨터 하드웨어 , 소프트웨어 사이를 연결하는 거대한 소프트웨어 시스템입니다. 하드웨어 리소스(CPU, 메모리, 디스크 등)를 효율적으로 관리하고, 응용 프로그램이 실행될 수 있는 환경을 제공하며 시스템의 보안을 책임집니다.

컴퓨터의 하드웨어 소프트웨어 사용자가 사용할 수 있게 하는 

시스템

운영체제가 어떧ㅎ게 제어하는지 
유튜버 와이어드 


2. 커널이란 무엇인가?
운영체제의 **핵심(Core)**이 되는 부분으로, 메모리에 상시 거주하며 하드웨어 제어와 프로세스 관리, 파일 시스템 관리 등 OS의 가장 기초적이고 중요한 기능을 수행하는 엔진입니다.

3. 스택(Stack) vs 힙(Heap) 메모리
메모리 공간을 관리하는 두 가지 주요 방식.
스택(Stack): 함수 호출 시 생성되는 지역 변수와 매개변수가 저장되는 공간입니다. LIFO(후입선출) 구조이며 컴파일 타임에 크기가 결정되고 자동으로 할당 및 해제되어 속도가 빠릅니다.
힙(Heap): 사용자가 직접 동적으로 할당하는 공간으로, 런타임에 크기가 결정됩니다. 가비지 컬렉터(GC)가 관리하거나 사용자가 직접 해제해야 하며, 스택보다 크지만 할당/해제 속도는 상대적으로 느립니다. 객체, 리스트 등
스택의 한계
할당의 
운영체제와 연관되어 공부해야함
<구체적인 사례로>

데이터 저장
구역이 두군데
스택 쌓아서 - 인트, 주소 
힙 빈곳에 데이터 - 리스트, 가비지 컬렉터 가 청소 

효율성-
속도-


4. 값 형식 vs 참조 형식
데이터가 메모리의 어디에, 어떤 방식으로 저장되는지에 따른 분류
값 형식: 변수에 실제 데이터 값 자체가 저장됩니다. (예: int, double) 다른 변수에 복사할 때 데이터 자체가 복제되므로 원본에 영향을 주지 않습니다.
참조 형식: 변수에 데이터가 저장된 메모리 주소가 저장됩니다. (예: Object, Class 인스턴스) 복사 시 주소만 전달되므로, 복사본의 값을 바꾸면 원본 데이터도 함께 바뀝니다.

5. Isolate의 사용 상황 및 주의점
상황: 고해상도 이미지 프로세싱, 대용량 JSON 파싱, 복잡한 수학 연산 등 CPU 집약적인 무거운 작업을 할 때 UI 스레드(Main Isolate)의 멈춤을 방지하기 위해 사용합니다.
주의점: Isolate는 메모리를 공유하지 않는 독립적 구조입니다. 따라서 데이터를 주고받을 때 메시지 복사(Serialization) 비용이 발생하며, 전역 변수를 공유할 수 없다는 특징을 반드시 고려해야 합니다.

6. 동기(Synchronous) vs 비동기(Asynchronous)
동기: 요청을 보낸 후 응답이 올 때까지 다음 작업을 하지 않고 기다리는 방식입니다. 순서가 보장되지만 대기 시간이 길어질 수 있습니다. 
비동기: 요청을 보낸 후 응답 여부와 상관없이 다음 작업을 즉시 수행하는 방식입니다. 효율적이지만 결과 처리를 위한 콜백이나 Future/Stream 관리가 필요합니다.

7. 블로킹(Blocking) vs 논블로킹(Non-blocking)
블로킹: 호출된 함수가 자신의 작업을 마칠 때까지 제어권을 갖고 있어, 호출한 쪽이 아무것도 못 하게 막는 상태입니다.
논블로킹: 호출된 함수가 작업을 다 마치지 않아도 제어권을 바로 돌려주어, 호출한 쪽이 다른 일을 할 수 있게 해주는 상태입니다.

8. 컴퓨터의 소수점 계산 방식 컴퓨터는 소수점을 부동 소수점(Floating Point) 방식으로 처리합니다. 정규화 2진법을 사용하기 때문에 $0.1 + 0.2$가 $0.30000000000000004$처럼 미세한 오차가 발생하며, 이를 위해 IEEE 754 표준을 주로 사용합니다.

9. 깊은 복사(Deep Copy) vs 얕은 복사(Shallow Copy)
얕은 복사: 객체의 참조 값(주소)만 복사합니다. 내부의 중첩된 객체들은 원본과 공유됩니다.
깊은 복사: 실제 값들을 메모리상에 새로운 공간으로 완전히 복제합니다. 원본과 복사본은 서로 완전히 독립된 객체가 됩니다.

10. 상속(Inheritance)
기존 클래스(부모)의 기능을 물려받아 새로운 클래스(자식)를 만드는 것입니다. 코드의 재사용성을 높여주지만, 계층이 너무 깊어지면 결합도가 높아질 수 있습니다.

11. 추상화(Abstraction)
복잡한 내부 구현은 숨기고 **사용자에게 필요한 핵심 기능(인터페이스)**만을 드러내는 것입니다. "무엇을 하는지"에 집중하고 "어떻게 하는지"는 하위 클래스에서 정의하도록 설계합니다.



12. 객체지향(OOP)이란?
프로그램을 단순한 명령어의 나열이 아닌, 독립된 단위인 '객체'들의 상호작용으로 파악하는 프로그래밍 패러다임입니다.
특징: 캡슐화(Encapsulation), 상속(Inheritance), 다형성(Polymorphism), 추상화(Abstraction)가 있습니다.

13. SOLID 원칙
객체지향 설계를 더 유지보수하기 쉽게 만드는 5가지 원칙입니다.
S (단일 책임): 클래스는 하나의 기능만 가져야 함.
O (개방-폐쇄): 확장은 열려 있고 수정은 닫혀 있어야 함.
L (리스코프 치환): 자식은 언제나 부모를 대체할 수 있어야 함.
I (인터페이스 분리): 사용하지 않는 메서드에 의존하도록 강제하지 않음.
D (의존역전 원칙): 구체화가 아닌 추상화에 의존해야 함.
마인드맵

1. 메모리의 종류와 여러 가지인 이유
메모리는 속도와 용량에 따라 **레지스터, 캐시, 메인 메모리(RAM), 보조 기억장치(HDD/SSD)**로 나뉩니다. 이렇게 계층이 나뉜 이유는 '비용 대비 효율' 때문입니다. 속도가 빠른 메모리는 비싸고 용량이 적으며, 느린 메모리는 저렴하고 용량이 크기 때문에 이들을 계층화하여 자주 쓰는 데이터는 빠른 곳에, 나머지는 저렴한 곳에 두는 메모리 계층 구조를 사용합니다.

2. 전역 변수 vs 정적 변수
전역 변수: 프로그램 어디서든 접근 가능하며, 프로그램 시작 시 할당되어 종료 시까지 유지됩니다.
정적 변수(Static): 선언된 범위(함수나 클래스 등) 내에서만 접근 가능하지만, 프로그램이 실행되는 동안 메모리 값을 유지합니다. 즉, 공유 범위는 제한적이되 생명주기는 프로그램 전체와 같습니다.

3. OS의 메모리 관리 이유와 전략
한정된 메모리 자원을 여러 프로세스가 효율적이고 안전하게 나눠 쓰게 하기 위함입니다.
반입 전략: 데이터를 언제 메모리로 가져올 것인가 (예: Demand Paging)
배치 전략: 데이터를 어디에 둘 것인가 (예: First-fit, Best-fit)
교체 전략: 메모리가 꽉 찼을 때 무엇을 내보낼 것인가 (예: LRU, FIFO)

4. 가상 메모리(Virtual Memory)란?
실제 물리적인 RAM 용량보다 큰 프로세스를 실행하기 위해, 보조 기억장치의 일부를 메모리처럼 사용하는 기술입니다. 이를 통해 각 프로세스는 자신만의 독립된 가상 주소 공간을 가지게 되어 보안과 효율성이 높아집니다.

5. 캐시 메모리와 적중률(Hit Ratio)의 원리
CPU와 RAM 사이의 속도 차이를 줄이기 위해 사용하는 고속 메모리입니다. 적중률을 높이기 위해 '참조 지역성(Locality)' 원리를 이용합니다.
시간 지역성: 최근에 접근한 데이터에 다시 접근할 가능성이 높음.
공간 지역성: 접근한 데이터 근처의 데이터에 접근할 가능성이 높음.

6. 메모리 고갈 시 현상
새로운 메모리 할당이 불가능해져 앱이 강제 종료되거나, OS가 시스템 보호를 위해 특정 프로세스를 강제로 죽이는 OOM(Out Of Memory) Killer가 작동합니다. 또한 가상 메모리 스와핑이 과도하게 발생하여 시스템 속도가 극도로 느려지는 트래싱(Thrashing) 현상이 나타납니다.

7. 가비지 컬렉터(GC)란?
프로그램이 동적으로 할당했던 메모리 영역(Heap) 중에서 더 이상 사용하지 않는 객체를 찾아 자동으로 해제해 주는 메모리 관리자입니다.

8. Dart의 가비지 컬렉터 작동 방식
Dart는 **세대별 가비지 컬렉션(Generational GC)**을 사용합니다.
Young Generation: 수명이 짧은 객체를 대상으로 자주, 빠르게 청소합니다. (Scavenging)
Old Generation: 오래 살아남은 객체들을 모아 가끔 정밀하게 청소합니다. (Mark-Sweep)
이 방식은 Flutter의 초당 60~120프레임 UI를 유지하기 위해 GC 중단 시간을 최소화하도록 설계되었습니다.

9. JSON이란 무엇인가?
JavaScript Object Notation의 약자로, 데이터를 주고받기 위해 사람이 읽기 쉬운 텍스트 형식으로 작성된 표준 데이터 포맷입니다. 키(Key)-값(Value) 쌍으로 이루어져 있어 대부분의 언어에서 쉽게 파싱할 수 있습니다.

10. 직렬화(Serialization) vs 역직렬화
직렬화: Dart의 객체(Class) 데이터를 네트워크 전송이 가능한 형태(주로 JSON 문자열)로 변환하는 과정입니다.
역직렬화: 받은 JSON 데이터를 다시 Dart에서 사용할 수 있는 객체로 변환하는 과정입니다.

11. HTTP의 특징과 문제점
특징: 무상태성(Stateless, 서버가 클라이언트 상태를 유지하지 않음), 비연결성(Connectionless, 요청/응답 후 연결 끊음)
문제점: 텍스트 그대로 전송되어 중간에 데이터가 도난당하거나 변조될 위험(보안 취약)이 있습니다.

12. HTTPS의 특징
HTTP에 SSL/TLS 암호화 레이어를 추가한 프로토콜입니다. 데이터를 암호화하여 전송하므로 중간자 공격을 방지하고, 인증서를 통해 접속하려는 서버가 진짜인지 확인(신뢰성)할 수 있습니다.

13. RESTful API란?
HTTP URI를 통해 **자원(Resource)**을 명시하고, HTTP Method(GET, POST, PUT, DELETE)를 통해 해당 자원에 대한 행위를 정의하는 아키텍처 스타일입니다. 메시지가 스스로를 설명해야 하며(Self-descriptive), 자원은 고유한 ID로 식별되어야 한다는 원칙을 따릅니다.

14. 서버와 클라이언트
클라이언트: 서비스를 요청하는 주체 (예: 스마트폰 앱, 브라우저)
서버: 요청을 받아 자원을 제공하거나 로직을 처리하는 주체.

15. 대칭키 vs 비대칭키 암호화
대칭키: 암호화와 복호화에 같은 키를 사용합니다. 속도는 빠르지만 키 전달 과정에서 탈취 위험이 있습니다.
비대칭키(공개키): 공개키와 개인키 한 쌍을 사용합니다. 공개키로 암호화하면 개인키로만 풀 수 있어 보안성이 매우 높지만 연산 속도가 느립니다.

16. "https://google.com" 요청 시 일어나는 과정
DNS 조회: 도메인 이름을 IP 주소로 변환합니다.
TCP 핸드쉐이크: 서버와 연결을 설정합니다.
SSL/TLS 핸드쉐이크: 보안 연결(암호화 키 교환)을 수립합니다.
HTTP 요청: 서버로 데이터를 요청합니다.
서버 응답: 서버가 데이터를 보냅니다.
렌더링: 앱/브라우저가 받은 데이터를 화면에 그립니다.

17. JWT(JSON Web Token)란?
인증에 필요한 정보들을 JSON 객체에 담아 암호화한 토큰입니다. 서버가 세션을 유지할 필요 없이 토큰 자체로 검증이 가능(Stateless)하므로 확장성이 좋아 로그인 인증에 자주 사용됩니다.

18. OAuth 2.0이란?
제3의 서비스(Google, Kakao 등)로부터 사용자의 데이터 접근 권한을 안전하게 위임받기 위한 권한 부여(Authorization) 표준 프로토콜입니다. 비밀번호를 직접 공유하지 않고 '액세스 토큰'을 통해 권한을 얻습니다.

19. OAuth 2.0 vs OIDC
OAuth 2.0: 권한(Authorization) 부여가 목적입니다. (예: 이 앱이 내 구글 드라이브에 파일을 올릴 수 있게 허용)
OIDC (OpenID Connect): OAuth 2.0 위에서 동작하며 인증(Authentication), 즉 "이 사용자가 누구인가"를 확인하는 신분증 역할을 추가한 것입니다.

1. 정렬 알고리즘이란 무엇이며, 사용 이유에 대해 설명해주세요.
정렬 알고리즘은 무작위로 나열된 데이터를 특정 기준(오름차순, 내림차순 등)에 따라 재배열하는 과정입니다. 정렬을 하는 이유는 탐색 효율성 때문입니다. 데이터가 정렬되어 있으면 **이진 탐색(Binary Search)**과 같은 효율적인 알고리즘을 사용할 수 있어, 수만 개의 데이터 중 원하는 값을 순식간에 찾아낼 수 있습니다.

2. 선택 정렬(Selection Sort) vs 버블 정렬(Bubble Sort)
선택 정렬: 남은 데이터 중 가장 작은 값을 찾아 맨 앞의 데이터와 자리를 바꾸는 방식입니다.
버블 정렬: 인접한 두 원소를 비교하여 더 큰 값을 뒤로 계속 보내는 방식입니다. 마치 거품이 올라오는 것 같아 버블 정렬이라 불립니다.

3. HashMap vs TreeMap 차이점| 구분 | HashMap | TreeMap || :--- | :--- | :--- || 정렬 여부 | 순서를 보장하지 않음 | 키(Key) 기준 자동 정렬 || 내부 구조 | 해시 테이블 (Hash Table) | 레드-블랙 트리 (이진 탐색 트리) || 시간 복잡도 | 평균 $(1)$ | 언제나 $O(\log n)$ |

4. 코루틴(Coroutine)이란 무엇인가요?
실행을 일시 중단(suspend)했다가 나중에 다시 시작(resume)할 수 있는 비선점형 멀티태스킹 메커니즘입니다.
유용한 상황: 네트워크 통신, 파일 읽기 등 시간이 걸리는 작업 시 스레드를 차단(Blocking)하지 않고 비동기 처리를 할 때 유용합니다. Dart의 async/await가 바로 코루틴의 개념을 차용한 것입니다.

5. 접근제한자(Access Modifier)란 무엇인가요?
클래스 내부의 데이터에 대한 접근 범위를 제한하여 캡슐화를 실현하는 장치입니다.
Public: 어디서든 접근 가능.
Private: 해당 클래스 내부에서만 접근 가능 (Dart에서는 변수명 앞에 _를 붙여 표시).
Protected: 상속받은 자식 클래스까지 접근 가능.

6. 람다식(Lambda Expression)이란 무엇인가요?
이름이 없는 익명 함수를 간결하게 표현한 식입니다. 함수를 변수에 담거나 다른 함수의 인자로 전달할 때 유용하며, 코드가 간결해지고 가독성이 좋아집니다.
예시: (x, y) => x + y;

7. 싱글톤 패턴(Singleton Pattern)이란 무엇인가요?
프로그램이 시작될 때 특정 클래스의 인스턴스를 단 하나만 생성하여 어디서든 동일한 인스턴스에 접근하도록 하는 디자인 패턴입니다.
사용 이유: 설정 정보, 데이터베이스 연결 객체 등 시스템 전반에서 공유해야 하는 자원을 일관되게 관리하기 위해 사용합니다. 하지만 전역 상태를 만들기 때문에 객체 간 결합도가 높아질 수 있다는 단점도 있습니다.


---
3/11
피드백: 다른사람에게 내가 알고있는 걸 설명한다는 느낌으로
        : 예제 코드라도 짜보고 직접 체득하기
        : 사용해본 코드 
        : 핵심키워드만 콕 찝어서, 그것만 떠오르면 술술 말할 수 있도록
        : 파생적으로 나오는 단어
        : 꼬리질문!!

3/12 
        : 동기 비동기 이미지로 그려보기
        : 긴장하면-> 모르겠다는 피해보기, 
                1. 솔직한 고백 머리속에 떠올리기, 
                2. 잘모르겠다 vs 뛰어나신 개발자앞에서서 생각이 안남 혹시 면접 합불을 떠나서 메일로 피드백을 받아도 될까요? -> 아첨? 재치? 센스? (사람 봐가면서)
        : 소수점 저장 -> 구체적으로 스택하나에 몇바이트고 어떻게 데이터가 저장되는지 ( 그림한번 그려보기 )
        : 비트코인과 이더리움에서 소수점 계산하는 방식 공부해보기
        : 유튜브 WIRED
        : 지피티한테 페르소나 주입, 어린이-청소년-대학생-박사
        : 핵심만 설명